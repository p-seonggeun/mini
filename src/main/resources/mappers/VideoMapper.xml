<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.goorm.mini.mapper.VideoMapper">

    <!-- SELECT -->
    <select id="selectIndex" resultType="io.goorm.mini.vo.Video">
        SELECT *
        FROM VIDEOS
        WHERE publish_yn = 1
        ORDER BY video_seq desc
    </select>

    <!-- SELECT -->
    <select id="selectAll" resultType="io.goorm.mini.vo.Video">
        SELECT * FROM VIDEOS
        ORDER BY video_seq desc
    </select>

    <select id="selectById" parameterType="long" resultType="io.goorm.mini.vo.Video">
        SELECT * FROM VIDEOS WHERE video_seq = #{videoSeq}
    </select>

    <!-- INSERT -->
    <insert id="insert" parameterType="io.goorm.mini.vo.Video">
        INSERT INTO VIDEOS (video, video_thumnail, member_seq)
        VALUES (#{video.video}, #{video.videoThumnail}, #{video.memberSeq})
    </insert>

    <!-- UPDATE -->
    <update id="update" parameterType="map">
        UPDATE VIDEOS
        SET video = #{video.video}, video_thumnail = #{video.videoThumnail}
        WHERE video_seq = #{videoSeq}
    </update>

    <!-- UPDATE -->
    <update id="updatePublish" parameterType="map">
        UPDATE VIDEOS
        SET publish_yn = #{video.publishYn}
        WHERE video_seq = #{videoSeq}
    </update>

    <!-- UPDATE -->
    <update id="updateDelete" parameterType="map">
        UPDATE VIDEOS
        SET delete_yn = #{video.deleteYn}
        WHERE video_seq = #{videoSeq}
    </update>

    <!-- DELETE -->
    <delete id="delete" parameterType="long">
        DELETE FROM VIDEOS WHERE video_seq = #{videoSeq}
    </delete>

</mapper>